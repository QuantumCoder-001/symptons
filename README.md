🏥 AI Symptom Checker & Disease Predictor"Turning Symptoms into Insights with Machine Learning"🎨 Project at a GlanceThis isn't just a script; it’s a predictive engine. It uses a Random Forest Classifier to analyze 132 different symptoms and predict the top 3 most likely health conditions.Brain Power: Random Forest ($n=100$).Accuracy: High-performance prediction on clinical datasets.Outputs: Returns Disease Name + Confidence Score (%).🛠️ The Developer's ToolkitIf you are building the web app, you need these three "golden files" generated by the model:FilePurposesymptom_model.pklThe trained AI "Brain".symptom_encoder.pklThe "Translator" (turns numbers into disease names).symptom_features.jsonThe "Map" (the exact list of 132 symptoms).🔌 Integration Logic (For the Frontend)To make the web app work, your friend needs to send data to the model in a very specific way. Think of it like a light switch board:[!IMPORTANT]The 132-Bit Rule: The model expects an array of exactly 132 numbers.1 = User has the symptom.0 = User does NOT have the symptom.The Workflow:Select: User picks symptoms (e.g., Itching, Fever) from a dropdown.Map: Match those names to their specific index in symptom_features.json.Predict: Send that binary array (0s and 1s) to the .predict_proba() function.💻 Sample Code for the Web AppHere is how the backend should handle the input:Python# Create the empty 'switchboard'
input_vector = np.zeros(132) 

# Flip the switches to 1 for symptoms the user selected
for symptom in user_selection:
    index = all_features.index(symptom)
    input_vector[index] = 1

# Get the results
prediction = model.predict_proba([input_vector])
